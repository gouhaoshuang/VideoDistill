[pytest]
# pytest 缓存目录
cache_dir = tests/.temp/.pytest_cache

# 测试文件匹配模式
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 覆盖率配置 - 指定要统计的源代码目录
addopts =
    --cov=src
    --cov-report=html:tests/.temp/htmlcov
    --cov-report=term-missing
    --cov-fail-under=80
