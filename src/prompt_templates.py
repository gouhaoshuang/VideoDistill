"""
提示词模板

定义用于视频分析、大纲生成、笔记生成和合并的提示词模板。
"""

# 大纲生成提示词
OUTLINE_PROMPT = """请分析这个视频，生成一个详细的大纲。

大纲应该包含：
1. 视频的主要主题和目的
2. 按时间或逻辑顺序分段，每段包含：
   - 段落编号和标题
   - 大致的时间范围（如果视频中有时序信息）
   - 该段的主要内容概述

请以清晰的层级格式输出，便于后续分段生成笔记。使用 Markdown 格式。"""

# 解析分段提示词
PARSE_SEGMENTS_PROMPT = """请将以下视频大纲解析为结构化的分段列表。

原始大纲：
{outline}

请返回 JSON 格式的分段列表，每个段落包含：
- id: 段落编号（从1开始）
- title: 段落标题
- description: 该段的详细描述
- time_range: 时间范围（如果有）

返回格式必须是有效的 JSON，不要包含其他说明文字：
[
  {{"id": 1, "title": "段落标题", "description": "描述", "time_range": "可选"}},
  ...
]"""

# 分段笔记生成提示词
SEGMENT_NOTE_PROMPT = """基于视频大纲，请为指定段落生成详细的笔记。

## 视频整体大纲
{outline}

## 当前需要生成笔记的段落
段落 {segment_id}: {segment_title}
描述: {segment_description}

请为这个段落生成详细的笔记，包括：
1. **关键概念**: 该段落介绍的核心概念和术语
2. **重要细节**: 具体的技术细节、步骤或要点
3. **示例或案例**: 如果有举例，请详细说明
4. **总结要点**: 该段落的要点总结

以 Markdown 格式输出，使用适当的标题层级、列表和格式化。笔记应该详细、准确、易于理解。
"""

# 笔记合并提示词
MERGE_NOTES_PROMPT = """请将以下分段笔记合并成一份完整、连贯的 Markdown 笔记文档。

## 分段笔记
{segments_notes}

## 合并要求
1. **添加总体摘要**: 在文档开头添加整段视频的总体摘要
2. **清晰的结构**: 使用适当的 Markdown 标题层级（#、##、###）
3. **过渡语句**: 在各段之间添加自然的过渡语句
4. **整体连贯性**: 确保文档逻辑连贯，阅读流畅
5. **总结**: 在文档结尾添加整体总结和关键要点回顾

请以专业的 Markdown 格式输出。"""

# 系统指令
SYSTEM_INSTRUCTION = """你是一个专业的视频内容分析师和笔记生成助手。
你的任务是：
1. 准确理解视频内容
2. 提取关键信息和要点
3. 生成结构清晰、内容详实的笔记
4. 使用 Markdown 格式输出
5. 确保内容准确、简洁、易读"""
